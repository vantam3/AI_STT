services:
  stt:
    build: .
    ports:
      - "9000:9000"
    env_file:
      - .env
    # Nếu dùng GPU (nvidia), bạn có thể bật:
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - capabilities: [gpu]
